import React from "react";
import { View, Text } from "react-native-animatable";

const Modal = () => {
    return (
        <View>
        <Text>Modal</Text>
        </View>
    );
};

export default Modal;

